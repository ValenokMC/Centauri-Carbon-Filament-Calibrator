# Third-party notices

This file matters more here than in most projects, because this tool sits next
to two large pieces of other people's software. It is worth being precise about
what is and is not redistributed.

## Runtime dependencies: none

The calibrator uses only the Python standard library. The release archive
contains this project's own source, its own generated plates, and nothing else.

## Python

The program needs a Python interpreter, which the user installs themselves.
Python is distributed under the
[PSF License Agreement](https://docs.python.org/3/license.html). Python is not
bundled.

## Development only

| Component | Used for | License |
|---|---|---|
| [pytest](https://pytest.org/) | Running the test suite | MIT |

Listed in `requirements-dev.txt`. Not shipped.

---

## OrcaSlicer

[OrcaSlicer](https://github.com/SoftFever/OrcaSlicer) is licensed under
**AGPL-3.0**.

**No OrcaSlicer code is included, linked, or redistributed in this project.**
The calibrator is a separate program that reads OrcaSlicer's configuration
files, writes a preset file into its profile folder, and launches it the way any
program launches another. It does not import, embed, or derive from its source.

### The calibration plate models

This is the part worth reading.

Five of the six plates per material — temperature tower, flow test, pressure
advance pattern, max volumetric flow tower, retraction tower — are **OrcaSlicer's
own calibration models**, produced by its built-in calibration wizard.

Those models ship with OrcaSlicer. The terms under which they may be
**redistributed separately from OrcaSlicer** are not stated anywhere this
project can point to.

Rather than guess, this project does not redistribute them:

- `Prepare-Templates.cmd` walks the user through running Orca's own calibration
  wizard once and saving the result. That happens on the user's machine, in the
  user's own copy of OrcaSlicer, which is already licensed to them.
- What the user saves is sanitised on the way in — the printer address and any
  personal preset names are removed — and stored in their own data directory. It
  never enters this repository or a release archive.
- The calibrator then personalises a copy of that plate per spool. The original
  is opened read-only and never modified.

**If you maintain OrcaSlicer** and would like to clarify the terms for these
models one way or the other, please
[open an issue](https://github.com/ValenokMC/Centauri-Carbon-Filament-Calibrator/issues).
Being able to ship them ready-made would remove a ten-minute step for every
user, and that would be a real improvement.

### The shrinkage plate, and the reference cube

These are generated by `src/centauri_calibrator/geometry.py` from numbers in
that file — a 100 × 10 × 3 mm bar and a plain cube, built vertex by vertex. They
are this project's own work, they ship in `templates/`, and they are covered by
this project's [MIT licence](LICENSE).

---

## Elegoo profiles

The Elegoo filament and process profiles are installed as part of OrcaSlicer and
belong to their authors. The calibrator **reads** them to find base values and
to follow the `inherits` chain. It does not copy them into this repository, and
it does not include their contents in a release.

The preset it writes is an **overlay**: it names an Elegoo profile in its
`inherits` field and adds only the measured values. It contains no Elegoo
profile content — which is also why an Elegoo profile update does not wipe a
calibration.

## The machine configuration

Building a full Orca project needs the machine configuration — several hundred
keys, including the real start G-code. That is read from **the user's own
OrcaSlicer installation** at run time and stored in their data directory. It is
not shipped, and it never could be: those values are specific to a printer and a
mistake in them means the nozzle striking the bed.

## Trademarks

"Elegoo" and "Centauri Carbon" are trademarks of their respective owner.
"OrcaSlicer" is the name of the project linked above. Both are used here
descriptively — to say what this software works with.

This project is **not affiliated with, endorsed by, or supported by** Elegoo or
the OrcaSlicer project.

## This project

Everything in `src/`, `tests/`, `tools/`, `docs/`, `examples/`, `assets/` and
`templates/` was written or generated for this project and is licensed under
[MIT](LICENSE).
